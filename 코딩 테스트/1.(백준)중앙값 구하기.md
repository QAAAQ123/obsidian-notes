https://www.acmicpc.net/problem/2696
## 문제
어떤 수열을 읽고, 홀수번째 수를 읽을 때 마다, 지금까지 입력받은 값의 중앙값을 출력하는 프로그램을 작성하시오.
예를 들어, 수열이 1, 5, 4, 3, 2 이면, 홀수번째 수는 1번째 수, 3번째 수, 5번째 수이고, 1번째 수를 읽었을 때 중앙값은 1, 3번째 수를 읽었을 때는 4, 5번째 수를 읽었을 때는 3이다.
## 입력
첫째 줄에 테스트 케이스의 개수 T(1 ≤ T ≤ 1,000)가 주어진다. 각 테스트 케이스의 첫째 줄에는 수열의 크기 M(1 ≤ M ≤ 9999, M은 홀수)이 주어지고, 그 다음 줄부터 이 수열의 원소가 차례대로 주어진다. 원소는 한 줄에 10개씩 나누어져있고, 32비트 부호있는 정수이다.
## 출력
각 테스트 케이스에 대해 첫째 줄에 출력하는 중앙값의 개수를 출력하고, 둘째 줄에는 홀수 번째 수를 읽을 때 마다 구한 중앙값을 차례대로 공백으로 구분하여 출력한다. 이때, 한 줄에 10개씩 출력해야 한다.

- 풀이
	- 중앙값의 개수: 배열 길이 / 2  + 1
	- 홀수번째(i)에서 멈출때마다 정렬하여 i에 arr[i/2]의 값을 출력한다
		- for문 안에서 정렬을 해야한다.
		- java의 Arrays.sort() 사용 -> 평균 : O(nlog(n)) / 최악 : O(n^2)
		- 총 시간 복잡도: O(n x nlog(n))
- 테스트 케이스만큼 반복을 해야하니 O(n)이 추가됨
- 최종 시간 복잡도: O(n x n x nlog(n))

- 탐색 문제이니까, 트리 형태의 자료구조 이용하면 더 빨라 질 수 있을 것 같음
- 트리에 하나씩 넣으면서 홀수 인덱스를 넣을 때 중앙값 구하는 방식

- 우선순위 큐 2개 사용
- 작은 수들은 최대 힙
- 큰 수들은 최소 힙
- 최대 힙 + 1 = 최소 힙 이거나 최대 힙 = 최소 힙
- 만약 최대 힙의 root가 최소 힙의 root 보다 크다면 root swap

```java
import java.util.*;

class Main{
	public static void main(String args[]) throws IOException{
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		int testCaseSize = Integer.parseInt(br.readLine());
		for(int i = 0; i < testCaseSize; i++){
			StringTokenizer st = new StringTokenizer(br.readLine());
			int eachArraySize = Integer.parseInt(st.nextToken());
			
			for(int j = 1; j <= eachArraySize; j++){
				int value = Integer.parseInt(st.nextToken());
			}
		}
	}
}
```
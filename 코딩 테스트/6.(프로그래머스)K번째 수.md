[링크](https://school.programmers.co.kr/learn/courses/30/lessons/42748)

## 1. 문제 이해
배열 array의 i번째 숫자부터 j번째 숫자까지 자르고 정렬했을 때, k번째에 있는 수를 구하려 합니다.
예를 들어 array가 [1, 5, 2, 6, 3, 7, 4], i = 2, j = 5, k = 3이라면
1. array의 2번째부터 5번째까지 자르면 [5, 2, 6, 3]입니다.
2. 1에서 나온 배열을 정렬하면 [2, 3, 5, 6]입니다.
3. 2에서 나온 배열의 3번째 숫자는 5입니다.
배열 array, [i, j, k]를 원소로 가진 2차원 배열 commands가 매개변수로 주어질 때, commands의 모든 원소에 대해 앞서 설명한 연산을 적용했을 때 나온 결과를 배열에 담아 return 하도록 solution 함수를 작성해주세요.

- commands가 2차원 배열로 여러개가 들어온다
- i,j로 자르고 k번째에 있는 숫자를 배열로 담아서 출력

## 2.입력 크기 및 시간/메모리 제한 확인
- array의 길이는 1 이상 100 이하입니다.
- array의 각 원소는 1 이상 100 이하입니다.
- commands의 길이는 1 이상 50 이하입니다.
- commands의 각 원소는 길이가 3입니다.

## 3. 로직 설계
- result 배열 만듦
- commands에서 i,j 읽어서 배열 만든 다음 정렬
- 정렬하고 k번째 찾아서 result에 넣음
- 반복
## 4. 자료구조 및 알고리즘
- 배열 사용 -> 시간복잡도 가장 낮은걸로 사용해야함

## 5. 복잡도 검증
- 한개 commands의 복잡도
	- i,j로 배열만들때 => O(N)
	- 정렬할때 java sort => O(Nxlog(N))
	- 더해야함
	- 결론: O(N x log(N))
- commands는 C개 있음 => O(C x N x log(N))
- $O(N \log N)$

## 6. 라이브러리 및 메소드 선택
- java arrays라이브러리
- Arrays.sort
- Arrays.copyOfRange

## 7. 검증
- 성공
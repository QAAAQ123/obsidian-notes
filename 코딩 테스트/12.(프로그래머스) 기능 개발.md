[링크](https://school.programmers.co.kr/learn/courses/30/lessons/42586)
#queue
## 1. 문제 이해
프로그래머스 팀에서는 기능 개선 작업을 수행 중입니다. 각 기능은 진도가 100%일 때 서비스에 반영할 수 있습니다.

또, 각 기능의 개발속도는 모두 다르기 때문에 뒤에 있는 기능이 앞에 있는 기능보다 먼저 개발될 수 있고, 이때 뒤에 있는 기능은 앞에 있는 기능이 배포될 때 함께 배포됩니다.

먼저 배포되어야 하는 순서대로 작업의 진도가 적힌 정수 배열 progresses와 각 작업의 개발 속도가 적힌 정수 배열 speeds가 주어질 때 각 배포마다 몇 개의 기능이 배포되는지를 return 하도록 solution 함수를 완성하세요.

- 한번에 배포할 수 있는 기능 개수 return
- 앞에 있는 것을 배포해야만 뒤에것 배포 가능

## 2.입력 크기 및 시간/메모리 제한 확인
- 작업의 개수(progresses, speeds배열의 길이)는 100개 이하입니다.
- 작업 진도는 100 미만의 자연수입니다.
- 작업 속도는 100 이하의 자연수입니다.
- 배포는 하루에 한 번만 할 수 있으며, 하루의 끝에 이루어진다고 가정합니다. 예를 들어 진도율이 95%인 작업의 개발 속도가 하루에 4%라면 배포는 2일 뒤에 이루어집니다.

- 길이(N) = 100이하

## 3. 자료구조 및 알고리즘
- 제일 먼저 들어온게 제일 먼저 나가는 자료구조=>FIFO
- Queue만 가능(우선순위 queue X)

## 4. 로직 설계
1. 매일마다 덧셈
	1. queue나 deque를 사용하더라도 맨 앞과 뒤만 볼 수 있기 때문에 불가능
2. 남은 일수를 큐에 넣음
	1. $100=진도+(속도 \times 남은일수)$ => $(100-진도)/속도 = 남은일수$(만약 정수가 아니면 1을 더해줘야함)
	2. 큐에서 꺼낼때마다 days에 지나간 일수를 더함
	3. days보다 작은 모든 value들을 꺼내서 셈함
	4. 반복

## 5. 복잡도 검증
- 큐에 넣을 떄:O(N)
- 큐에서 뺄때: O(N)
- 결론:O(N)

## 6. 라이브러리 선택
* ArrayDeque
* Deque(인터페이스)
* Math.ceil

## 7. 검증
```java
import java.util.*;

class Solution {
    public int[] solution(int[] progresses, int[] speeds) {
        List<Integer> answer = new ArrayList<>();
        Queue<Integer> bucket = new ArrayDeque<>();
        
        for(int i = 0;i < speeds.length; i++){
            bucket.offer((int)Math.ceil((double)(100-progresses[i])/speeds[i]));
        }
        
        while(!bucket.isEmpty()){
            int days = bucket.poll();
            int count = 1;
            while(!bucket.isEmpty() && days >= bucket.peek()){
                bucket.poll();
                count++;
                
            }
            answer.add(count);
            count = 0;
        }
        
        
        
        return answer.stream().mapToInt(i -> i).toArray();
    }
}



```
## 8. 몰랐던 메소드 정리
- Queue<> name = new ArrayDeque<>() => queue 만드는 방법
- .offer(value): queue에 집어넣기
- .poll(): queue에서 빼고 값 확인
- .peek(): queue에서 빼지 않고 값 확인
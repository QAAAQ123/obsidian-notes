[링크](https://school.programmers.co.kr/learn/courses/30/lessons/87946)

## 1. 문제
XX게임에는 피로도 시스템(0 이상의 정수로 표현합니다)이 있으며, 일정 피로도를 사용해서 던전을 탐험할 수 있습니다. 이때, 각 던전마다 탐험을 시작하기 위해 필요한 "최소 필요 피로도"와 던전 탐험을 마쳤을 때 소모되는 "소모 피로도"가 있습니다. "최소 필요 피로도"는 해당 던전을 탐험하기 위해 가지고 있어야 하는 최소한의 피로도를 나타내며, "소모 피로도"는 던전을 탐험한 후 소모되는 피로도를 나타냅니다. 예를 들어 "최소 필요 피로도"가 80, "소모 피로도"가 20인 던전을 탐험하기 위해서는 유저의 현재 남은 피로도는 80 이상 이어야 하며, 던전을 탐험한 후에는 피로도 20이 소모됩니다.

이 게임에는 하루에 한 번씩 탐험할 수 있는 던전이 여러개 있는데, 한 유저가 오늘 이 던전들을 최대한 많이 탐험하려 합니다. 유저의 현재 피로도 k와 각 던전별 "최소 필요 피로도", "소모 피로도"가 담긴 2차원 배열 dungeons 가 매개변수로 주어질 때, 유저가 탐험할수 있는 최대 던전 수를 return 하도록 solution 함수를 완성해주세요.

## 2. 문제 이해
- 던전 하나는 한번만 탐험 가능
- 모든 경우의 수를 따져봐야함->brute force

## 3.입력 크기 및 시간/메모리 제한 확인
- k는 1 이상 5,000 이하인 자연수입니다.
- dungeons의 세로(행) 길이(즉, 던전의 개수)는 1 이상 8 이하입니다.
    - dungeons의 가로(열) 길이는 2 입니다.
    - dungeons의 각 행은 각 던전의 ["최소 필요 피로도", "소모 피로도"] 입니다.
    - "최소 필요 피로도"는 항상 "소모 피로도"보다 크거나 같습니다.
    - "최소 필요 피로도"와 "소모 피로도"는 1 이상 1,000 이하인 자연수입니다.
    - 서로 다른 던전의 ["최소 필요 피로도", "소모 피로도"]가 서로 같을 수 있습니다.

## 4. 최대 시간복잡도 확인
- 던전의 최대 개수=8 => 재귀로 풀어야함
- O(N!)

## 5. 자료구조 및 알고리즘
- DFS
## 6. 로직 설계
- 탐험한 최대 던전의 개수를 담아놓는 maxDun생성=>answer로 대체
- DFS로 완전 탐색
- k = health
- isClear: boolean으로 탐색완료한 던전 표시하는 배열

```text
DFS(isClear, dungeons, count, health)
	if(dunCount > maxDun) maDun = dunCount
	for(i,i>dungeons.lenght,i++)
		if(health < dungeons[i][0] && isClear[i] = false)
			  isClear[i] = true
			  dfs(isClear,dungeons,count++,health-dungeons[i][1])
			  isClear[i] = false
```



### 6.1 예외 케이스
- 없음

## 7. 시간복잡도 검증
DFS => O(N!)

## 8. 코드 검증
```java
class Solution {
    static int answer = 0;
    public int solution(int k, int[][] dungeons) {
        boolean isClear[] = new boolean[dungeons.length];
        
        dfs(isClear,dungeons,0,k);
        return answer;
    }
    
    private void dfs(boolean[] isClear,int[][] dungeons, int count,int k){
        answer = Math.max(count,answer);
        
        for(int i = 0; i < dungeons.length; i++){
            if(k >= dungeons[i][0] && isClear[i] == false){
                isClear[i] = true;
                dfs(isClear,dungeons,count+1,k-dungeons[i][1]);
                isClear[i] = false;
            }
        }
    }
}

```
## 9. 몰랐던 내용 정리
- java는 boolean배열을 false로 초기화 한다

|타입|기본값|
|---|---|
|boolean|false|
|int,long,short,byte|0|
|double,float|0.0|
|char|`\u0000`(공백)|
|참조형(String)|null|

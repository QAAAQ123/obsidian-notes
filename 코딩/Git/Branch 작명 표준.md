## ✅ 브랜치명 작명 규칙

### 1. **작업 유형 접두어 사용**

작업의 목적을 명확히 하기 위해 접두어를 붙입니다:

| 접두어         | 의미          |
| ----------- | ----------- |
| `feature/`  | 새로운 기능 개발   |
| `fix/`      | 버그 수정       |
| `hotfix/`   | 긴급 수정       |
| `refactor/` | 코드 리팩토링     |
| `test/`     | 테스트 관련 작업   |
| `chore/`    | 잡무, 설정 변경 등 |
| `docs/`     | 문서 작업       |

---

### 2. **kebab-case 사용 권장**

Git 브랜치명은 URL 경로로도 사용되기 때문에 **하이픈(-)**으로 단어를 구분하는 `kebab-case`가 가장 일반적입니다.

예시:

```plaintext
feature/login-page
fix/user-auth-error
refactor/comment-service
```

---

### 3. **이슈 번호 포함 (선택 사항)**

Jira, GitHub Issues 등과 연동할 경우 이슈 번호를 포함하면 추적이 쉬워집니다:

```plaintext
feature/123-add-login-page
fix/456-invalid-token-error
```

---

### 4. **의미 있는 이름 사용**

브랜치명만 보고도 작업 내용을 유추할 수 있도록 구체적으로 작성합니다:

❌ `feature/update`  
✅ `feature/update-user-profile`

---

## 🧠 브랜치명 예시 모음

```plaintext
feature/create-post-api
fix/comment-pagination-bug
refactor/user-service-layer
docs/update-readme
chore/setup-eslint-config
```

---

## 📌 팁

- 공백, 특수문자, 대문자 사용은 피하세요
- 너무 길지 않게 (보통 3~5단어 이내)
- 팀 내 컨벤션 문서로 정리해두면 협업에 유리

- test 코드에서 오류
```java
when(mockPostRepository.findById(postId)).thenReturn(existingPost);
//existingPost를 Optional.of(existingPost)로 변경해야 한다.
```
- JpaRepositty는`Optional<T> findById(ID id);`로 구성되어있다. 따라서 findById는 Optional값을 반환해야 한다. 

[[Optional 클래스]]
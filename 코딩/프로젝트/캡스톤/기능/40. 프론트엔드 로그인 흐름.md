## 게스트 로그인
```java
@Operation(summary = "게스트 로그인", description = "게스트로 로그인을 진행합니다.")  
@ApiResponses(value = {  
        @ApiResponse(responseCode = "200", description = "Accees Token 전송 및 Refresh Token,게스트 정보 저장 완료")  
})  
@PostMapping("/guest/login")  
public ResponseEntity<LoginDto.Response> guestLogin(@RequestBody LoginDto.GuestRequest guestRequest, HttpServletResponse response) {  
    log.info("신규 게스트 로그인 요청: {}", guestRequest.nickname());  
  
    Tokens tokens = loginService.guestLogin(guestRequest);  
  
    //1. Refresh Token을 HttpOnly Cookie에 굽는다  
    ResponseCookie responseCookie = ResponseCookie.from("refreshToken", tokens.refreshToken())  
            .httpOnly(true)  
            .secure(true)  
            .path("/")  
            .maxAge(Duration.ofDays(14))  
            .sameSite("None")  
            .build();  
    response.addHeader(HttpHeaders.SET_COOKIE, responseCookie.toString());  
  
    //2. Access Token과 Role을 Http Body에 담아서 전송한다.  
    log.info("신규 게스트 계정 생성 완료");  
    return ResponseEntity.status(HttpStatus.OK)  
            .body(LoginDto.Response.success(tokens));
}
```

```text
공통 요청
Post: /auth/guest/login
Header: 없음
Body: {페르소나(CAT,FOX,BEAR,PANDA),언어(kor,eng)}

공통 응답 Body
{
	accessToken: "xxxxx",
	role: "ROLE_GUEST",
	nickname: "xxxxx",
	persona: "xxxx",
	lang: "xxx" 
}
```
### 1. 게스트 계정이 없는 신규 게스트
1. 게스트 로그인 버튼 클릭 
2. Local Storage에 accessToken이 있는지 확인(신규 가입이니까 없음)
3. 페르소나 선택 페이지로 넘어감
4. 페르소나 선택하면 Body에 담아서`${baseUrl}/auth/guest/login`으로 요청을 보냄
5. 응답으로 {accessToken,role,nickname,persona,lang}이 돌아옴
6. role이 ROLE_GUEST이기 때문에 accessToken을 Local Storage에 넣음

### 2. 게스트 계정이 있는 기존 게스트
1. 게스트 로그인 버튼 클릭
2. Local Storage에 accessToken이 있는지 확인(이미 가입했으니까 있음)
3. accessToken에서 페이로드의 exp를 확인하여 만료되었는지 확인
	1. 만료되지 않았으면 현재 accessToken 그대로 사용하여 로그인
	2. 만료되었으면 `/auth/refresh`요청을 보내서 accessToken 재발급하여 로그인 처리하고 LocalStorage에 넣음




## 유저(구글/카카오) 로그인

```java
@Operation(summary = "카카오/구글 로그인", description = "카카오/구글 SDK 로그인 로직을 수동으로 진행하여 상황에 맞는 토큰을 생성하여 전송 및 저장합니다.")  
@ApiResponses(value = {  
        @ApiResponse(responseCode = "200", description = "가입을 하지 않은 사용자 상세 정보 입력을 위한 토큰 발급"),  
        @ApiResponse(responseCode = "200", description = "가입을 이미 완료한 사용자를 위한 토큰 발급")  
})  
@PostMapping("/user/login")  
public ResponseEntity<?> socialLogin(@RequestBody LoginDto.UserRequest userRequest, HttpServletResponse response,@AuthenticationPrincipal CustomOAuth2User user) {  
    Tokens tokens = loginService.processSocialLogin(userRequest,user);  
  
    //1. 신규 가입자  
    if("ROLE_PENDING".equals(tokens.role())) {  
        //1.1 게스트 출신 신규 가입자(user가 null이 아님)  
        if(user != null){  
            Map<String,String> guestProfile = loginService.getProfileInRedis(user.getIdentifier());  
            log.info("게스트 계정 있는 사용자 소셜 로그인 요청 성공");  
            return ResponseEntity.status(HttpStatus.OK)  
                    .body(LoginDto.Response.pending(  
                            tokens.accessToken(),  
                            tokens.role(),  
                            guestProfile  
                    ));  
        }  
  
        // 1.2 게스트 계정이 없는 신규 가입자(nser가 null임)  
        log.info("게스트 계정이 없는 사용자 소셜 로그인 요청 성공");  
        return ResponseEntity.status(HttpStatus.OK)  
                .body(LoginDto.Response.pending(  
                        tokens.accessToken(),  
                        tokens.role(),  
                        null  
                ));  
    }  
  
  
    //2. 이미 가입완료한 사용자-access/refresh token 모두 발급  
    //refresh token 쿠키에 등록  
    ResponseCookie responseCookie = ResponseCookie.from("refreshToken", tokens.refreshToken())  
            .httpOnly(true)  
            .secure(true)  
            .path("/")  
            .maxAge(Duration.ofDays(14))  
            .sameSite("None")  
            .build();  
    response.addHeader(HttpHeaders.SET_COOKIE, responseCookie.toString());  
    //accessToken과 role을 return    log.info("이미 가입 완료한 사용자 로그인 성공");  
    return ResponseEntity.status(HttpStatus.OK)  
            .body(LoginDto.Response.success(tokens));  
  
}
```

### 1. 게스트로 가입하지 않고 바로 소셜 로그인하는 사용자
```text
요청
Post: /auth/user/login
Header: 없음
Body: {code,provider(google,kakao)}

응답 Body
{
	accessToken: "xxxxx",
	role: "ROLE_PENDING"
}

요청2
POST: /api/onboarding/complete
Header: Authorization: Bearer {accessToken}  ROLE_PENDING인 access token임
Body: {mbti,birthdate,sasnag,gender,.....}

응답 Body
{
	accessToken: "xxxxx",
	role: "ROLE_USER"
}
```
1. 구글/카카오 로그인 버튼 클릭
2. 각 제공자에 맞는 SDK 방식으로 code를 받는 요청을 보냄
	1. 구글 리다이렉트 URI: `https://2025-capstone-design-fe.vercel.app/auth/google/callback`
	2. 카카오 리다이렉트 URI: `https://2025-capstone-design-fe.vercel.app/auth/kakao/callback`
3. 리다이렉트 URI로 받은 `code`와 `OAuth제공자(google/kakao)`를 Http Body{code,provider}에 담아서 `${baseUrl}/auth/user/login` 로그인 요청한다.
4. 응답으로 {accessToken,role}이 돌아옴(role="ROLE_PENDING")
5. ROLE_PENDING이기 때문에 초기 정보 입력 화면으로 넘어감
6. 체질,MBTI,생년월일 등을 입력을 완료함
7. Http Body에 사용자가 입력한 데이터를 담아서 `${baseUrl}/api/onboaridng/complete`로 요청을 보냄
8. 응답으로 {accessToken,role과 사용자 입력 데이터}가 돌아옴(role="ROLE_USER")
9. `${baseUrl}/api/onboaridng/complete`의 응답의 role로 ROLE_USER가 오면 Local Storage에 access token이 있는지 확인
10. ROLE_GUEST인 토큰이 없으니까 넘어감

### 2. 게스트 계정이 있는 상태에서 소셜 로그인하는 사용자
```text
요청
Post: /auth/user/login
Header: Authorization: Bearer {accessToken}  ROLE_GUEST인 access token임
Body: {code,provider(google,kakao)}

응답 Body
{
	accessToken: "xxxxx",
	role: "ROLE_PENDING",
	nickname: "xxxx",
	persona: "xxxx",
	lang: "xxx"
}

요청2
POST: /api/onboarding/complete
Header: Authorization: Bearer {accessToken}  ROLE_PENDING인 access token임
Body: {mbti,birthdate,sasnag,gender,.....}

응답 Body
{
	accessToken: "xxxxx",
	role: "ROLE_USER"
}
```
1. 구글/카카오 로그인 버튼 클릭
2. 각 제공자에 맞는 SDK 방식으로 code를 받는 요청을 보냄
	1. 구글 리다이렉트 URI: `https://2025-capstone-design-fe.vercel.app/auth/google/callback`
	2. 카카오 리다이렉트 URI: `https://2025-capstone-design-fe.vercel.app/auth/kakao/callback`
3. 리다이렉트 URI로 받은 `code`와 `OAuth제공자(google/kakao)`를 Http Body{code,provider}에 담아서 `${baseUrl}/auth/user/login` 로그인 요청한다.
4. 응답으로 {accessToken,role,nickname,persona,lang}이 돌아옴(role="ROLE_PENDING")
5. ROLE_PENDING이기 때문에 초기 정보 입력 화면으로 넘어감
6. 체질,MBTI,생년월일 등을 입력을 완료함-
7. Http Body에 사용자가 입력한 데이터를 담아서 `${baseUrl}/api/onboaridng/complete`로 요청을 보냄
8. 응답으로 {accessToken,role과 사용자 입력 데이터}가 돌아옴(role="ROLE_USER")-★★★이때 /user/login에서 받은 nickname,persona는 사용자가 입력하지 않아도 자동으로 들어가게 해야함★★★
9. 8. 응답으로 {accessToken,role과 사용자 입력 데이터}가 돌아옴(role="ROLE_USER")
10. `${baseUrl}/api/onboaridng/complete`의 응답의 role로 ROLE_USER가 오면 Local Storage에 access token이 있는지 확인
11. ROLE_GUEST인 토큰이 있으니까 Local Storage에서 삭제함


### 3. 이미 가입을 완료한 사용자가 로그인 요청
```text
요청
Post: /auth/user/login
Header: 없음
Body: {code,provider(google,kakao)}

응답 Body
{
	accessToken: "xxxxx",
	role: "ROLE_USER"
}
```
1. 구글/카카오 로그인 버튼 클릭
2. 각 제공자에 맞는 SDK 방식으로 code를 받는 요청을 보냄
	1. 구글 리다이렉트 URI: `https://2025-capstone-design-fe.vercel.app/auth/google/callback`
	2. 카카오 리다이렉트 URI: `https://2025-capstone-design-fe.vercel.app/auth/kakao/callback`
3. 리다이렉트 URI로 받은 `code`와 `OAuth제공자(google/kakao)`를 Http Body{code,provider}에 담아서 `${baseUrl}/auth/user/login` 로그인 요청한다.
4. 응답으로 {accessToken,role}이 돌아옴(role="ROLE_USER")
5. `${baseUrl}/auth/user/login`의 응답의 role이 ROLE_USER이니까 메인 지도 화면으로 이동함


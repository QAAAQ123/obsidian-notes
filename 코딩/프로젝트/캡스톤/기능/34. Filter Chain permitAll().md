## Spring security config에 permitAll()로 허용을 해놓은 엔드포인트
- permitAll()은 Filter Chian을 건너뛰는게 아니다
- 필터를 모두 거친후에 마지막 권한 검사 단계에서 무조건 통과시키는 것이다.
- 만약 필터 자체를 아예 안 타게 만들고 싶다면 `WebSecurityCustomizer`에서 `.ignoring()`을 사용해야 한다
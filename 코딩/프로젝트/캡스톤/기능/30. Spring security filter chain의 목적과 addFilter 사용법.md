#securityfilterchain

## 목적
- Spring security filter chain의 주 목적은 **'이미 발급된 토큰이나 자격 증명의 유효성을 검사'** 하는 것이다.
- 따라서 [[29. 사용자 편의를 위한 게스트 로그인 추가]]에서 게스트를 위한 jwt를 만들때에는 SecurityConfig에서 addFilter를 통해 일반 jwt 로그인/회원가입을 추가하지 않아야 한다.
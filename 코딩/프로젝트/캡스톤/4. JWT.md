#JWT
## JWT 구조
1. Header: 토큰 타입(JWT), 서명 알고리즘 정보
2. Payload: Claim(사용자 정보, 만료 시간 등)
3. Signature: Header + Payload를 secret key로 서명한 값

## JWT 전송
- JWT는 보통 **HTTP Header(Authorization: Bearer …)** 또는 **쿠키**에 담겨 전송됩니다. Base64 URL-safe 인코딩 덕분에 URL 파라미터로도 전달할 수 있지만, 보안상 권장되진 않습니다.
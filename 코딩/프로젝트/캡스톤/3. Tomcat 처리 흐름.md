1. Clinet 요청
2. Web Server(Tomcat)이 Raw HTTP를 받음
3. Tomcat이 Raw HTTP를 HttpServletRequest(Java Object)로 변환
4. HttpServletRequest을 Filter가 가로채서 검사(Spring security등)
5. 보안 검사를 통과하면 Tomcat이 DispatcherServlet에 HttpServeltRequest를 넘김
6. DispatcherServlet이 알맞는 Controller를 찾아냄
7. Controller 실행
#servlet
#MongoRepository #MongoTemplate

## Spring Data MongoDB가 DB에 접근하는 방식
1. MongoRepository(인터페이스 방식)
	1. 쿼리 자동 생성
	2. 쉽지만, 복잡한 쿼리나 부분 수정에 한계
2. MongoTemplate(클래스 방식)
	1. 쿼리,조건,수정 객체를 직접 조립해서 실행
	2. 복잡한 연산,정교한 수정,집계 등을 수행할 때 사용
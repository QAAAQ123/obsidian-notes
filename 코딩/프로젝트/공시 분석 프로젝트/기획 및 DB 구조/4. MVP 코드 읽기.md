1. get_best_available_model: gemini모델 리스트와 모델 선택
	1. genai의 list_models에서 지원하는 모델만 뽑아서 available_modles에 넣음
	2. print로 available_models목록을 보여줌
	3. 순위
		1. flash 모델
		2. pro 모델
		3. 제일 첫번째 모델
2. get_gemini_sumarry(text): text를 AI로 분석함
	1. get_best_available_model로 모델 받아옴
	2. 모델로 모델 생성(GenerativeModel)
	3. 프롬프트 + text 입력
	4. 모델이 응답해준 테스트 리턴
3. get_google_risk_direct: 분석할 10-k 가져와서 risk부분만 떼어옴
	1. url과 SEC_HEADER를 조합해서 get 요청 보냄
	2. 가져온 json을 data에 저장
	3. data에서 10-k를 가져오는데 필요한 문자열들을 따로 저장함
	4. 저장한 문자열을 조합해서 sec.gov로 html읽어오는 get 요청 보냄
	5. BeautifulSoup로 파싱해서 soup에 저장
	6. text만 fulltext에 저장
	7. fulltext는 너무 길어서 뒤쪽 십만자만 떼어서 리턴

### 결론:filing 목록 -> HTML파일 -> AI분석 요청 -> 응답 view
1. cik.json으로 티커의 filing 목록 가져옴
2. 가져온 filing목록에서 10-k 요청 보내기 위한 값만 뽑음
3. 뽑은 값과 sec.gov/....조합해서 문서 전체 가져옴
4. 텍스트 전체중에 뒤쪽 10만자만 떼어냄
5. 10만자 중에서 위험 부분만 AI한테 요청보냄
6. 응답 받은 텍스트를 화면에 보여줌
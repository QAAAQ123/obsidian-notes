## 1. 요청 파라미터
1. `@RequestParam`
2. `@PathVariable`
3. `@MatrixVariable`

## 2. 폼 태그
1. `@ModelAttribute`로 바인딩
2. `@InitBinder`로 커스텀 데이터 바인딩

## 3. 파일 업로드(이미지 등)
MultipartFile이용

## 4. 다국어 처리
1. MessageSource 이용
2. LocaleResolver와 LocaleChangeInterceptor를 이용해 언어 변경

## 5. 유효성 검사
1. Bean Validation 이용
2. ConstrationValidator 인터페이스 이용
3. Validator 인터페이스 이용

## 6. 예외 처리
1. `@ResponseStatus`를 이용한 HTTP 상태코드 기반 예외 처리
2. `@ExceptionHandler`를 이용한 지역 예외 처리
3. `@ControllerAdvice`를 이용한 전역 예외 처리

## 7. 로그
1. Log4j
2. Log4j2
3. Logback
4. java.util.logging

- 위 4가지 로깅 도구들을 추상화한 SLF4J를 이용하여 로깅
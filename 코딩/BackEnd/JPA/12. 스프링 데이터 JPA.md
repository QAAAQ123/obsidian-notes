## 스프링 데이터 JPA
- 스프링 프레임워크에서 JPA를 편리하게 사용할 수 있도록 지원하는 프로젝트
- CURD 반복 문제 해결
- 데이터 접근 계층을 개발할 때 구현 클래스 없이 인터페이스만 작성해도 개발을 완료 할 수 있다
- org.springframework.data.jpa.repository.JpaRepository인터페이스를 구현해서 개별 repository를 만든다
- 스프링 데이터(Repository<- CurdRepository<-PagingAndSortingRepository)<- 스프링 데이터 JPA(JpaRepository)

## JpaRepository의 주요 메소드
T:엔티티,ID:식별자 타입,S:엔티티와 그 자식 타입
1. save(S): 새로운 엔티티 저장하고 이미 있는 엔티는 수정
2. delete(T): 엔티티 하나를 삭제한다. 내부에서 em.remove()를 호출한다.
3. findOne(ID): 엔티티 하나를 조회한다. 내부에서 EntityManager.find()를 호출한다.
4. getOne(ID): 엔티티 프록시로 조회한다. 내부에서 EntityManger.getReference()를 호출한다
5. findAll(...): 모든 엔티티를 조회한다. 정렬이나 페이징 조건을 파라미터로 제공할 수 있다.

## 쿼리 메소드 기능
1. 메소드 이름으로 쿼리 생성
2. JPA NamedQuery: 메소드 이름으로 JPA NamedQuery 쿼리를 호출하는 기능
3. 파라미터 바인딩
4. 벌크성 수정 쿼리
5. 반환 타입
6. 페이징과 정렬
7. 힌트

## Lock
쿼리 시 락을 걸려면 org.springframework.data.jpa.repository.Lock 어노테이션을 사용하면 된다

## 명세
????모르겠음

## 사용자 정의 리포지토리 구현
- 리포지토리 인터페이스의 이름을 인터페이스 이름+Impl로 지어야 한다.

### Web 확장
- 도메인 컨버터 기능
- 페이징과 정렬 기능
- 페이징 정보가 둘 이상이면 접두사 사용하여 구분




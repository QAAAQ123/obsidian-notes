- Depends 클래스는 FastAPI의 의존성 주입을 담당한다.
- Depends는 함수를 인수로 사용하거나 함수 인수를 라우트에 전달 할 수 있게 해서 의존성을 확보해준다.

```python
async def create_event(new_event: Event,  session = Depends(get_session ) 
```
- 위 함수는 get_session에 의존하도록 설정한 함수이다->get_session과 get_session()은 다름(JS의 일급객체??와 같은 개념)